const { calcularMDC } = require('./exercicio_mdc')

describe('Funções Matemáticas', () => {

    it('Validar o cálculo do MDC de 12 e 18', () => {
        expect(resultado = calcularMDC(15, 20))
    });
});